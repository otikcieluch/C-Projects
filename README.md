# C-Projects
My C Projects.

install raylib -> sudo pacman -S raylib
  from the extra repo
run -> gcc cproject.c -o swarm -lraylib -lGL -lm -lpthread -ldl -lrt -lX11 //to compile
run -> ./swarm //to run
